# GovFiles data API key. Create one at https://govfiles.dev (dashboard: https://govfiles.dev/dashboard/).
# The client sends this value as the X-API-Key header. Leave it blank here and export the real key
# in your shell, or copy this file to .env (which is gitignored) and fill it in locally.
GOVFILES_API_KEY=
